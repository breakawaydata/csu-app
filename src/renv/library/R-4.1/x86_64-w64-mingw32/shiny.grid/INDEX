areaBasedRules          Generates a list of rules for a single grid
                        attribute based on the given grid areas.
collapseValues          Takes a list of values and collapses them into
                        a single valid css string.
generateGridRule        Generates a css rule for the given id element.
getAttributeStyle       Generates all required css for the given css
                        attribute based on the grid rules provided.
gridPage                Create a page a with CSS grid layout
gridPanel               Create a panel with a CSS grid layout
hello                   Hello, World!
preprocessAttribute     Converts string type attributes into a named
                        list. Does nothing if the attribute is already
                        a list in the correct format.
preprocessContent       Adds a css class to any HTML elements from the
                        content that are named.
